const config=require('../config/index')
function request({url,method='GET',data,loading=false}){if(loading)wx.showLoading({title:'稍等一下',mask:true});return new Promise((resolve,reject)=>wx.request({url:`${config.apiBaseUrl}${url}`,method,data,header:{'Content-Type':'application/json',Authorization:wx.getStorageSync('token')?`Bearer ${wx.getStorageSync('token')}`:''},success(response){const body=response.data||{};if(response.statusCode===401){wx.removeStorageSync('token');wx.removeStorageSync('user')}if(response.statusCode>=200&&response.statusCode<300&&body.code===0)resolve(body.data);else{const error=new Error(body.message||'网络好像开小差了');error.statusCode=response.statusCode;reject(error)}},fail(){reject(new Error('网络好像开小差了'))},complete(){if(loading)wx.hideLoading()}}))}
module.exports={request}
