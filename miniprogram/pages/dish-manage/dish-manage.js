const api=require('../../services/dishes');Page({data:{dishes:[],keyword:'',searchOpen:false,loading:true},onShow(){this.load()},sort(list){return list.slice().sort((a,b)=>Number(b.orderedCount||0)-Number(a.orderedCount||0)||String(a.name).localeCompare(String(b.name),'zh-Hans-CN'))},apply(){const keyword=this.data.keyword.trim().toLowerCase(),dishes=this.sort(this._all||[]).filter(item=>!keyword||[item.name,item.description,item.categoryName,...(item.tags||[])].join(' ').toLowerCase().includes(keyword));this.setData({dishes})},async load(){try{this._all=await api.list({includeDisabled:true});this.apply();this.setData({loading:false})}catch(e){wx.showToast({title:e.message,icon:'none'})}},searchToggle(){this.setData({searchOpen:!this.data.searchOpen,keyword:''},()=>this.apply())},input(e){this.setData({keyword:e.detail.value},()=>this.apply())},add(){wx.navigateTo({url:'/pages/dish-edit/dish-edit'})},categories(){wx.navigateTo({url:'/pages/category-manage/category-manage'})},edit(e){const dish=this.data.dishes.find(d=>d.id===e.currentTarget.dataset.id);if(!dish.enabled)return;wx.navigateTo({url:`/pages/dish-edit/dish-edit?id=${dish.id}`})},async disable(e){const r=await new Promise(resolve=>wx.showModal({title:'把这道菜收起来？',content:'点过次数和历史记录都会保留。',success:resolve}));if(!r.confirm)return;try{await api.disable(e.currentTarget.dataset.id);this.load()}catch(err){wx.showToast({title:err.message,icon:'none'})}}})
